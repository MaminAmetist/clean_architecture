# Clean Architecture API (Чистая архитектура)

[![FastAPI](https://img.shields.io/badge/FastAPI-005571?style=for-the-badge&logo=fastapi)](https://fastapi.tiangolo.com)
[![Python](https://img.shields.io/badge/Python-3.9+-blue?style=for-the-badge&logo=python)](https://python.org)

Этот проект представляет собой **шаблон Чистой архитектуры (Clean Architecture)** на базе FastAPI, предложенной Робертом Мартином (Uncle Bob). Это наиболее строгий и дисциплинированный подход к организации кода, который обеспечивает максимальную независимость бизнес-логики от внешних фреймворков, баз данных и пользовательских интерфейсов.

## 🎯 Что такое Чистая архитектура?

Чистая архитектура — это архитектурный паттерн, целью которого является создание систем с высокой степенью поддерживаемости, тестируемости и независимости от внешних факторов. Она очень похожа на Луковую архитектуру (Onion Architecture) и Гексагональную архитектуру (Hexagonal Architecture), разделяя многие из тех же принципов.

**Основная идея:** организация кода в виде концентрических кругов, где внутренние круги содержат более абстрактную и важную бизнес-логику, а внешние круги содержат конкретные детали реализации и внешние зависимости.
```text
    ┌─────────────────────────────────────┐
    │     Фреймворки и Драйверы           │
    │  ┌───────────────────────────────┐  │
    │  │    Интерфейсные Адаптеры      │  │
    │  │  ┌─────────────────────────┐  │  │
    │  │  │  Сценарии использования │  │  │
    │  │  │      (Use Cases)        │  │  │
    │  │  │  ┌───────────────────┐  │  │  │
    │  │  │  │    Сущности       │  │  │  │
    │  │  │  │   (Entities)      │  │  │  │
    │  │  │  └───────────────────┘  │  │  │
    │  │  └─────────────────────────┘  │  │
    │  └───────────────────────────────┘  │
    └─────────────────────────────────────┘
```

## 🔄 Главное правило — Dependency Rule (Правило зависимостей)

**Зависимости всегда должны быть направлены внутрь, от внешних кругов к внутренним. Код во внутреннем круге никогда не должен знать о существовании кода во внешнем круге.**

Это достигается за счет использования **принципа инверсии зависимостей** и определения **абстракций (интерфейсов) во внутренних кругах**, которые реализуются во внешних.


## 🗂️ Структура проекта (строгая Clean Architecture)
```text
clean_architecture/
├── app/
│ ├── init.py
│ ├── main.py # Главный файл FastAPI
│ │
│ ├── presentation/ # 🔴 САМЫЙ ВНЕШНИЙ КРУГ: Фреймворки и Драйверы (часть)
│ │ ├── init.py
│ │ ├── dependencies.py # Зависимости для роутеров
│ │ └── routers/ # FastAPI роутеры (Контроллеры)
│ │ ├── init.py
│ │ ├── categories.py
│ │ └── posts.py
│ │
│ ├── infrastructure/ # 🟠 ВНЕШНИЙ КРУГ: Фреймворки и Драйверы (основная часть)
│ │ ├── init.py
│ │ ├── persistence/ # Реализации хранилищ
│ │ │ ├── init.py
│ │ │ └── sqlalchemy/ # Конкретная реализация на SQLAlchemy
│ │ │ ├── init.py
│ │ │ ├── models.py # SQLAlchemy ORM-модели (отдельно от доменных!)
│ │ │ ├── category_repository.py # Реализация ICategoryRepository
│ │ │ └── post_repository.py # Реализация IPostRepository
│ │ └── database/ # Настройка подключения к БД
│ │ ├── init.py
│ │ ├── dependencies.py
│ │ └── connection.py
│ │
│ ├── application/ # 🟡 СРЕДНИЙ КРУГ: Интерфейсные Адаптеры + Сценарии использования
│ │ ├── init.py
│ │ ├── services/ # Сценарии использования (Use Cases)
│ │ │ ├── init.py
│ │ │ ├── categories.py
│ │ │ └── posts.py
│ │ └── schemas/ # Pydantic схемы (DTO)
│ │ ├── init.py
│ │ ├── category.py
│ │ └── post.py
│ │
│ ├── domain/ # 🟢 САМЫЙ ВНУТРЕННИЙ КРУГ: Сущности (Entities)
│ │ ├── init.py
│ │ ├── models/ # Чистые доменные сущности (НЕ зависят от ORM!)
│ │ │ ├── init.py
│ │ │ ├── category.py # Чистый Python-класс / dataclass
│ │ │ └── post.py # Чистый Python-класс / dataclass
│ │ └── repositories/ # Абстрактные интерфейсы (Порты)
│ │ ├── init.py
│ │ ├── categories.py # ICategoryRepository (абстрактный класс)
│ │ └── posts.py # IPostRepository (абстрактный класс)
│ │
└── requirements.txt
```

## 🧩 Ключевое отличие от Луковой архитектуры

В этом проекте мы сделали **важное архитектурное решение**, которое приближает нас к строгой Чистой архитектуре:

### Было (в Луковой архитектуре):
```python
# app/domain/models/category.py (Зависит от SQLAlchemy!)
from sqlalchemy import Column, Integer, String
from app.infrastructure.database import Base

class Category(Base):  # 👈 Зависимость от инфраструктуры!
    __tablename__ = "categories"
    id = Column(Integer, primary_key=True)
    name = Column(String)
```    
    
### Стало (в Чистой архитектуре):
```python
# app/domain/models/category.py (Чистый Python!)
from dataclasses import dataclass
from typing import Optional

@dataclass
class Category:  # 👈 Никаких зависимостей! Чистая доменная сущность
    id: Optional[int] = None
    name: str = ""
```
```python
# app/infrastructure/persistence/sqlalchemy/models.py (ORM-модель)
from sqlalchemy import Column, Integer, String
from app.infrastructure.database import Base

class CategoryModel(Base):  # 👈 ORM-модель живет в инфраструктуре
    __tablename__ = "categories"
    id = Column(Integer, primary_key=True)
    name = Column(String)
```

### 🔄 Поток данных с преобразованием
Теперь репозитории берут на себя ответственность за преобразование между ORM-моделями и доменными сущностями:

```text
1. HTTP-запрос → [Presentation Router]
2. [Router] → вызывает [Service (Use Case)]
3. [Service] → работает с чистыми доменными сущностями
4. [Service] → вызывает [Repository Interface] (порт)
5. [Repository Implementation] (адаптер) → преобразует:
   Доменная сущность ←→ ORM-модель
6. [ORM Model] ←→ [Database]
```

Пример работы репозитория:
```python
# Репозиторий преобразует ORM-модель в доменную сущность
def get_by_id(self, category_id: int) -> Optional[Category]:
    orm_category = self.db.query(CategoryModel).filter(CategoryModel.id == category_id).first()
    if orm_category:
        # 🔄 Преобразование ORM → Доменная сущность
        return Category(id=orm_category.id, name=orm_category.name)
    return None

def create(self, category: Category) -> Category:
    # 🔄 Преобразование Доменная сущность → ORM
    orm_category = CategoryModel(name=category.name)
    self.db.add(orm_category)
    self.db.flush()
    # Возвращаем доменную сущность с присвоенным ID
    return Category(id=orm_category.id, name=orm_category.name)
```
## 📚 Детальное описание кругов (слоев)
1. 🟢 Сущности (Entities) — domain/models/
Самый внутренний круг. Содержит основные бизнес-правила и сущности предметной области.

Характеристики:

- Чистые Python-классы или dataclasses

- Никаких зависимостей от фреймворков, БД, ORM

- Содержат бизнес-методы (если необходимо)

- Наиболее стабильная часть приложения

2. 🟡 Сценарии использования (Use Cases) — application/services/
Содержит логику конкретных операций приложения.

Пример: "Создать категорию", "Получить список постов"

Характеристики:

- Оркестрирует поток данных к и от Сущностей

- Использует интерфейсы репозиториев (порты)

- Не знает о деталях реализации БД или API

- Зависит только от Сущностей

3. 🟠 Интерфейсные Адаптеры (Interface Adapters) — application/schemas/ + infrastructure/persistence/
Преобразуют данные между внешним миром и внутренними кругами.

Включает:

Шлюзы (Gateways): Реализации репозиториев (адаптеры к БД)

Представители (Presenters): Pydantic схемы для форматирования ответов

Контроллеры (Controllers): Роутеры в presentation/

4. 🔴 Фреймворки и Драйверы (Frameworks & Drivers) — presentation/ + infrastructure/database/
Самый внешний круг. Содержит все конкретные детали реализации.

Включает:

FastAPI: Обработка HTTP-запросов/ответов

SQLAlchemy: Конкретная реализация доступа к данным

База данных: Физическое хранилище

## ✨ Преимущества Чистой архитектуры
|Преимущество|	Описание|
|:---|:---|
|🔌 Независимость от фреймворков|	Ядро ничего не знает о FastAPI, SQLAlchemy и т.д.
|🧪 Тестируемость|	Use Cases тестируются без БД, просто мокая репозитории
|🔄 Заменяемость БД	|Можно перейти с PostgreSQL на MongoDB, изменив только инфраструктурный слой
|📦 Независимость от UI|	Можно добавить CLI или Telegram-бота, не меняя ядро
|⏳ Долговечность	|Бизнес-логика остается стабильной при смене технологий
|👥 Масштабирование команды	|Четкие границы позволяют разделять работу

## 🚀 Быстрый старт
Клонируйте репозиторий:
```bash
git clone https://github.com/MaminAmetist/clean_architecture.git
cd clean_architecture
```

Создайте и активируйте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows
```

Установите зависимости:
```bash
pip install -r requirements.txt
```

Запустите сервер:
```bash
uvicorn app.main:app --reload
```
Изучите документацию:
Перейдите по адресу http://127.0.0.1:8000/docs

## 📊 Сравнение архитектурных подходов
|Аспект|	Многоуровневая|	Модульная|	Луковая|	Чистая|
|:---|:----|:----|:----|:---|
Основной принцип|	Технические слои	|Бизнес-модули	|Слои с инверсией	|Концентрические круги
Изоляция БД	|Частичная (репозитории)|	Внутри модуля|	Через интерфейсы	|Полная (отдельные модели)
Сложность|	Низкая|	Средняя|	Высокая	|Очень высокая
Тестируемость ядра|	Средняя|	Средняя|	Высокая|	Максимальная
Когда выбирать|	Простые проекты|	Средние проекты|	Сложные проекты	|Критически важные системы
ORM в домене|	Да	|Да|	Да (может быть)|	НЕТ! 🚫

## 🎯 Когда выбирать Чистую архитектуру?
Чистая архитектура не нужна для:

- Простых CRUD-приложений
- MVP (Minimum Viable Product)
- Проектов с жесткими сроками

Чистая архитектура необходима для:

- Систем с долгим жизненным циклом (5+ лет)

- Сложных бизнес-доменов с запутанной логикой

- Проектов, где БД может измениться

- Приложений, критичных к тестированию

- Систем, где разные команды работают над разными частями

## 📖 Заключение
Этот проект демонстрирует строгую реализацию Чистой архитектуры с полным разделением доменных моделей и ORM. Хотя такой подход требует больше кода и дисциплины, он обеспечивает максимальную независимость ядра приложения от внешних технологий и фреймворков.

Главное отличие от предыдущих архитектур: Доменные сущности теперь абсолютно чисты и не знают о способе хранения. Это позволяет менять БД, ORM и даже язык программирования, сохраняя бизнес-логику нетронутой.